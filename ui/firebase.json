{
  "hosting": {
    "public": "dist",
    "_comment": "no node_modules ignore: expo exports the icon fonts under assets/node_modules, and ignoring that path served the app with empty squares where every icon should be",
    "ignore": ["firebase.json", "**/.*"],
    "cleanUrls": true,
    "trailingSlash": false,
    "rewrites": [{ "source": "**", "destination": "/index.html" }],
    "headers": [
      {
        "source": "/_expo/**",
        "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
      },
      {
        "_comment": "the page itself is never cached: every deploy changes which bundle it points at, and a stale copy asks for files that no longer exist. The bundle's own name carries its hash, so it can be cached for good.",
        "source": "**/*.@(html|json)",
        "headers": [{ "key": "Cache-Control", "value": "no-cache, must-revalidate" }]
      },
      {
        "source": "/",
        "headers": [{ "key": "Cache-Control", "value": "no-cache, must-revalidate" }]
      }
    ]
  }
}
